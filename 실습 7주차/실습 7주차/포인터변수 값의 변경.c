/*
#include <stdio.h>

int main(void) {
	int num1 = 10;
	int num2 = 20;

	int* p1 = &num1; // 포인터 변수에 는 &를 이용하여 주소를 넣어줘야함
	int* p2 = &num2;
	int* temp;

	printf("p1 참조값 : %d\n", *p1);
	printf("p2 참조값 : %d\n", *p2);

	temp = p1;
	p1 = p2;
	p2 = temp;

	printf("swap 후 p1의 참조값 : %d\n", *p1);
	printf("swap 후 p2의 참조값 : %d\n", *p2);



	return 0;
}
*/

