/*#include <stdio.h>
int main(void) {
	int x, y, r;
	printf("두개의 정수를 입력하시오(큰수, 작은수) :");
	scanf_s("%d %d",&x, &y);
	while (y != 0) {
		r = x % y;
		x = y;
		y = r;
	}
	printf("최대 공약수는 %d입니다.", x);
	return 0;
}*/
//어떤 입력을 넣었는지 알려주시면 더 자세한 도움을 드릴 수 있습니다. 
//하지만 일단 코드상에 문제는 없어 보입니다. 다만 scanf_s 함수에서 정수 입력값 사이에 쉼표가 들어가지 않도록 주의하세요.

