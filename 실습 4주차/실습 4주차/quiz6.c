/*#include <stdio.h>
int main(void) {
	int num, sum;
	sum = 0;
	printf("number: ");
	scanf_s("%d", &num);
	for (int i = 0; i <= num; i++) {
		if (i % 2 == 0) {
			sum += i;
		}
	}
	printf("The sum of even numbers (0~%d) = %d", num, sum);
	return 0;
}*/

// 짝수는 2로 나누었을때 나머지가 0인게 이문제의 포인트. 그리고 sum을 왜 초기화 해야 하는지 물어보기

