/*
#include <stdio.h>
void fac(int x) {
	int sum = 1;
	for (int i = 1; i <= x; i++) {
		sum *= i;
	}
	printf("%d", sum);
}
		
int main(void) {
	int x;
	printf("input: ");
	scanf_s("%d", &x);
	fac(x);
	return 0;
}
*/

/*
quiz 1 을 다시 푼 문제. 나는 void로 받았고 for문안에 i를 2부터 시작하는게 더 보기 좋은듯
*/