
/*
#include <stdio.h>
void gugu(int x) {
	for (int i = 0; i < 9; i++) {
		printf("%d * %d = %d\n", x, i + 1, x * (i + 1));
	}
	printf("\n");
	//return 0; 이거 작성할 필요 없음. void로 반환형 정했으면 return 값이 없는거니 return 0 작성 굳이 필요 x
}


int main(void)  {
	int x;
while (1) {
	printf("X = Multiples of 2\n");
	printf("X = 1\n");
	printf("X = Other\n");
	printf("Please Input X: ");
	scanf_s("%d", &x);
	if (x % 2 == 0) {                   // (!(x%2)) 와 같음   !(n%2) , n%2 == 0 은 같음   !(n%3) , n%3 == 0 은 같음
		printf("Multiplication of 2\n");
		gugu(x);
	}
	else if (x == 1) {
		break;
	}
	else {
		printf("%d is ODD NUMBER\n\n", x);
	}
}
	return 0;
}
*/
	
